"""Public financial report collection & traceable research analysis.

A vertical capability package built on top of DB-GPT, providing a complete
chain: public data discovery -> parsing -> metric extraction -> provenance
tracking -> analysis -> chart & cited report.
"""

__version__ = "0.1.0"
